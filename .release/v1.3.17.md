# Snap-Lite v1.3.17

- 新增标准 Windows 安装器 `SnapLite-Setup-1.3.17.exe`。
- 安装时可自行选择安装目录，自动安装 `SnapLite.exe` 与 `SnapLiteOCR.exe` 到同一位置。
- 安装器创建开始菜单入口，并提供可选桌面快捷方式。
- 自动生成卸载程序，后续更新可直接覆盖安装。
- Release 同时保留 ZIP 便携版，方便无需安装的用户。
- 继续包含 RapidOCR 高精度本地 OCR 与 v1.3.16 的统一浅粉白 UI。
- 安装器使用构建阶段生成的合法多尺寸图标资源。

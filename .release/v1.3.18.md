# Snap-Lite v1.3.18

- 回退 RapidOCR / ONNX / PyInstaller 伴随进程方案，彻底移除 `SnapLiteOCR.exe`。
- 恢复轻量的 Windows 本地 OCR（`Windows.Media.Ocr`），继续保留放大、灰度增强、二值化和按识别行恢复换行。
- 保留 v1.3.16 之后的统一浅粉白 UI、OCR 右侧结果面板、选择性复制、橡皮和扩展撤销历史。
- 主程序重新回到单个 `SnapLite.exe` 即可完整运行，不需要额外模型和运行库。
- Release 同时提供单独 exe、轻量安装包和单 exe ZIP 便携版。

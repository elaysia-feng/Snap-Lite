# Snap-Lite v1.3.12

- 新增“取字”按钮：识别当前截图选区中的文字，并自动复制到剪贴板。
- 使用 Windows 系统自带 OCR，本地离线识别，不调用外部网络服务。
- OCR 语言跟随 Windows 已安装的语言/OCR 包，支持中文、英文等系统可用语言。
- “取字”按钮继续使用现有紧凑二次元角色按钮样式和悬浮提示。
- OCR 接入 C++/WinRT，并保持原有截图、橡皮、撤销/重做、保存流程不变。

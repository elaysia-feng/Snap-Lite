# Snap-Lite v1.3.19

- 优化撤销/重做历史内存占用，不再长期保存大量原始 HBITMAP 快照。
- 最近 2 步保持原始位图，保证常用 Ctrl+Z / Ctrl+Y 响应速度。
- 更早的历史步骤自动使用无损 PNG 压缩后保存在内存中。
- 保留最多 128 步历史，同时将每个 Undo/Redo 栈控制在约 64MB。
- 继续保留单 exe、Windows 本地 OCR、橡皮、统一浅粉白 UI 和现有功能。

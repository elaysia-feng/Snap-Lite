# Snap-Lite v1.3.13

- 提升“取字”准确率：截图选区会先进行 2x/3x 放大，再分别使用原色、灰度增强、自动二值化三种图像进行 OCR。
- OCR 会优先使用系统已安装的简体中文识别器，并结合用户默认语言和英文识别器进行多次识别，自动选择质量更高的结果。
- 提取完成后不再强制自动复制，也不再弹“复制成功”消息框。
- 在截图选区右侧新增“提取文字”结果窗口，识别内容可直接鼠标拖选。
- 支持 Ctrl+C 复制选中文字，并提供“复制选中”“复制全部”“关闭”按钮。
- 结果窗口默认显示在选区右侧；右侧空间不足时自动切换到左侧，避免超出屏幕。

Snap-Lite v1.3.3-beta.3

Beta test focus:
- Fix stale selection-frame ghosting during fast drag/resize by forcing synchronous repaint while the snip window owns mouse capture.
- Fix transparent text editor stale glyphs after Backspace/Delete/Cut/Clear by restoring the capture background and repainting the EDIT control immediately.

Release recovery:
- Reuse the existing immutable v1.3.3-beta.3 tag if it already exists.
- Create the missing GitHub Release and upload fresh Windows x64 assets.

# Snap-Lite v1.3.3-beta.6

Beta test focus:
- Rebuild the text edit refresh path instead of stacking the Beta.3-Beta.5 repaint patches.
- Attach text refresh behavior only to real Windows Edit controls, never the screenshot parent.
- Resize both the text annotation host and Edit from live input so long Chinese text is not clipped to a few characters.
- Keep native Edit behavior for caret movement, IME, Backspace and Delete.
- Remove hide/show and focus-changing repaint behavior.
- Restore captured pixels below edited text without intercepting the Edit WM_ERASEBKGND path.
- Keep deterministic embedded application icon verification in Windows CI and release builds.
